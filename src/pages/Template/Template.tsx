import React from "react";
import Layout from "../../components/Layout/Layout";

const Template = () => {
  return (
    <Layout title="Template">
      <p>
        This is just an example of how to setup pages to all look consistent and
        be configured the same
      </p>
    </Layout>
  );
};

export default Template;
